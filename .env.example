# Database Configuration
# This URL format is designed for the Docker Compose setup.
# The 'db' hostname refers to the database service in the Docker network.
DATABASE_URL="mysql://root:mysecretpassword@db:3306/company_db"

# Server Configuration
PORT=3000
NODE_ENV=development
r